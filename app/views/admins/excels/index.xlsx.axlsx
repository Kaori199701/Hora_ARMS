wb = xlsx_package.workbook

@attendances.each do |attendance|
  wb.add_worksheet(name: attendance.worker.full_name) do |sheet|
    sheet.add_row [ "workerID", "苗字", "名前" ]
    sheet.add_row [ attendance.worker.id, attendance.worker.last_name, attendance.worker.first_name ]
  end
end
