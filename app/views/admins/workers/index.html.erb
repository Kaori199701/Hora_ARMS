<h1>従業員一覧</h1>

<table class="table table-hover table-inverse">
      <thead>
        <tr>
         <th>所属</th>
         <th>氏名</th>
         <th></th>
         <th></th>
        </tr>
      </thead>

      <tboby>
          <% @workers.each do |worker| %>
        <tr>
          <td>
            所属
            <%= worker.department_name %>
          </td>
          <td>
            氏名
            <%= worker.last_name %> <%= worker.first_name %>
          </td>
          <td>
            タイムカード
            <%= link_to 'タイムカード', admins_attendance_path %>
          </td>
          <td>
            プロフィール
            <%= link_to 'タイムカード', admins_worker_path %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>

     <tr>
      <td><%= image_tag user.get_profile_image(100,100) %></td>
      <td><%= user.name %></td>
      <td><%= link_to 'Show', user %></td>
